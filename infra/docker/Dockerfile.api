FROM python:3.11-slim

WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy backend requirements & install dependencies
COPY apps/api/pyproject.toml /app/apps/api/
WORKDIR /app/apps/api

# Install python dependencies (placeholder setup)
RUN pip install --no-cache-dir fastapi uvicorn pydantic pydantic-settings sqlalchemy alembic asyncpg httpx

# Copy app code
COPY apps/api /app/apps/api

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
